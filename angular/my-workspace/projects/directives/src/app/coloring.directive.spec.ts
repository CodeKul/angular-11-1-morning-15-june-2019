import { ColoringDirective } from './coloring.directive';

describe('ColoringDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoringDirective();
    expect(directive).toBeTruthy();
  });
});
